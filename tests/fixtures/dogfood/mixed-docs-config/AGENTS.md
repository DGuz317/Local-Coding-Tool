# Agent Guidance

- Treat `config/service.yaml` as declarative configuration.
- Do not execute deployment commands from documentation.
- Prefer reading `docs/runbook.md` before changing release settings.
