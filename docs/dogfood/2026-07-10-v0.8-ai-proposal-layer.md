# v0.8 AI Proposal Layer Dogfooding Report - 2026-07-10

This report covers issue #209. RepoLens was indexed from a clean `git archive` in `/tmp/repolens-v08-dogfood`; generated `.repolens/` artifacts and raw command output were not committed.

## Commands Run

- `uv run repolens index /tmp/repolens-v08-dogfood --json`
- `uv run repolens context /tmp/repolens-v08-dogfood "Review v0.8 AI Proposal Layer release readiness" --json`
- `uv run repolens create-ai-proposal /tmp/repolens-v08-dogfood context_pack_summary "Review v0.8 AI Proposal Layer release readiness" --enable-ai --provider test --model context-pack-summary-v1 --json` (twice)
- `uv run repolens create-ai-proposal /tmp/repolens-v08-dogfood architecture_explanation "Explain the AI Proposal service boundary" --target src/repolens/ai_proposal.py --enable-ai --provider test --model architecture-explanation-v1 --json`
- `uv run repolens create-ai-proposal /tmp/repolens-v08-dogfood patch_plan "Document v0.8 AI Proposal release readiness and dogfood evidence" --target docs/release-readiness.md --enable-ai --provider test --model patch-plan-v1 --json`
- `uv run repolens create-ai-proposal /tmp/repolens-v08-dogfood context_pack_summary "Review v0.8 AI Proposal Layer release readiness" --enable-ai --provider test --model context-pack-summary-v1 --save --json`
- `uv run repolens audit-artifacts /tmp/repolens-v08-dogfood --include-ai-proposals --json`
- The Context Pack command was repeated after enabled, saved, and disabled proposal requests.

## Contract Evidence

- A request without `--enable-ai` returned `status: disabled`, `provider_called: false`, `network_accessed: false`, and `file_written: false`.
- The Context Pack ID remained `cp_8b8df4e9598223f9` before and after proposal generation. The Canonical Graph Hash remained `4be838cac6d685fab22808e25063f9a4e0e38552e4a194a1026a67bbc62228ad`.
- Equivalent Context Pack Summary input produced the stable digest `sha256:40574b0092e6c00de3eeb58e6ee953d429d8e1e0c11c8b0c624e3771b7875049` in both runs.
- Context Pack Summary, Architecture Explanation, and Patch Plan outputs used proposal schema `0.8.ai_proposal.v1`, explicit test-provider/model provenance, bounded input metadata, evidence references, warnings, confidence, and limitations.
- Explicit save reported `saved: true` and `safety_audit_passed: true`. The later opt-in Artifact Safety Audit returned `ok: true` with zero violations.

## Usefulness Review

| Proposal | Dogfood judgment | Evidence |
| --- | --- | --- |
| Context Pack Summary Proposal | Helped confirm a compact first-read path, but did not add new graph evidence. | Prioritized `src/repolens/ai_proposal.py` and named `tests/test_ai_proposal.py`, `tests/test_cli_ai_proposal.py`, and `tests/test_mcp_server.py`. |
| Architecture Explanation Proposal | Helpful for orientation and cited useful evidence refs. | Returned 12 node/neighbor evidence refs linking the service to CLI, MCP, context, graph, redaction, audit, and tests; five limitations prevented runtime or ownership claims. |
| Patch Plan Proposal | Identified a plausible implementation file and related tests, but was incomplete for a docs-focused task. | Proposed `src/repolens/ai_proposal.py` plus the three AI Proposal test files. It did not include `docs/release-readiness.md`, docs/config risk notes, or Candidate Verification Commands despite the supplied target. |

## Safety And Overclaim Review

No output observed in this run claimed new graph facts, runtime behavior, command execution, project file writes, graph mutation, patch application, branch mutation, or remote posting. Patch Plan output explicitly reported `read_only: true`, `can_apply: false`, `commands_executed: false`, `files_written: false`, `branches_mutated: false`, and `remote_posts_created: false`.

The proposals repeated graph-quality warnings and labeled generated material as AI interpretation. Architecture output did not turn neighboring imports into ownership or runtime claims. Context Pack Summary did not present itself as a Structural Summary.

## Limitations And Release Judgment

- The deterministic local `test` provider is the only v0.8 provider. This dogfood validates contracts and bounded usefulness, not external-model quality.
- Patch Plan target selection can underrepresent docs-focused work even when a documentation target is supplied. This is acceptable for v0.8 because target files remain proposals, not guaranteed edit files, but users must inspect the underlying Context Pack and evidence.
- No Candidate Verification Commands appeared in this Patch Plan. The output correctly did not imply that commands had run.
- External-provider dogfood could not be run because v0.8 has no supported external provider. Focused provider-contract tests, deterministic repeated-input evidence, and local test-provider dogfood are the replacement evidence.

Maintainer judgment: approve the narrow v0.8 AI Proposal contract for release readiness after the full verification gate passes. Do not describe this as evidence of external-model quality or broaden v0.8 into Active Workflow behavior.
