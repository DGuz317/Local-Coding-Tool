# v0.4 JS/TS Workspace Dogfooding Report - 2026-06-30

This report covers issue #128. Generated `.repolens/` artifacts were inspected locally and are intentionally not committed. The target is a committed distilled JS/TS workspace fixture, not a vendored third-party repository snapshot.

## Commands Run

- `uv run repolens index tests/fixtures/dogfood/js-ts-workspace --json`
- `uv run repolens context tests/fixtures/dogfood/js-ts-workspace "Trace @dog/lib workspace import and package ownership" --json`
- `uv run repolens evaluate-context --json`

## Coverage

| Target | Category | Result |
| --- | --- | --- |
| `tests/fixtures/dogfood/js-ts-workspace` | Distilled JS/TS workspace | Indexed successfully; Context Pack returned successfully |

## Findings

- The fixture represents explicit package evidence through root `package.json`, workspace package `package.json` files, and scoped `tsconfig.json` alias configuration.
- `uv run repolens index tests/fixtures/dogfood/js-ts-workspace --json` returned `ok: true`, indexed 9 eligible files, and skipped only generated `.repolens/` artifacts.
- `uv run repolens context tests/fixtures/dogfood/js-ts-workspace "Trace @dog/lib workspace import and package ownership" --json` returned `ok: true` with confidence `medium`.
- The Context Pack surfaced evidence-backed package boundaries for `@dog/app` and `@dog/lib` from package manifest evidence.
- Candidate verification commands remained not run and included separate risk buckets: `vitest run` as `verification_likely` and `tsc -b` as `quality_check_likely`.
- v0.4 evaluation passed the package/workspace gate for `v0_4_js_ts_workspace_package_import`, including package boundary checks for `@dog/app` and `@dog/lib`, command risk buckets, expansion, no source snippets, not-run commands, and repo-relative paths.
- Expected safe-failure behavior is to preserve unresolved or ambiguous relationships as Relationship Candidates plus Graph Quality Warnings instead of creating false definitive graph edges.
- Context Pack output for this scenario should surface structured package/workspace metadata, relationship candidates, warning codes, related configs, and command risk buckets without source snippets, code bodies, raw config values, raw comments, or paragraph excerpts.

## Known Limitations

- Unsupported workspace declarations remain outside the v0.4 contract unless represented by explicit supported local evidence.
- Complex package entrypoint maps, generated entrypoints, framework-specific resolution, and package-manager execution are not emulated.
- Unresolved aliases, ambiguous `baseUrl` matches, and aliases used outside the applicable config subtree remain unresolved or candidates.
- Lockfile-only evidence does not create package ownership facts.

## Actionable Regressions And Fixtures

- Keep `tests/fixtures/dogfood/js-ts-workspace` as the release-readiness dogfood fixture for package identity, workspace membership, package ownership, workspace import resolution, and scoped TypeScript alias orientation.
- Keep v0.4 expectation gates in `tests/fixtures/context_pack/evaluation_manifest.json` as the release-blocking regression suite for package/workspace Context Pack behavior.
- `uv run repolens evaluate-context --json` returned `ok: true`, release gate passed, and 19/19 cases passed.
- No third-party source snapshots or generated `.repolens/` artifacts are committed.

## Release Gate Status

- This report satisfies the issue #128 dogfooding record requirement without exposing source content from an external repository.
- Full verification gate and Context Pack Evaluation results are recorded in `docs/release-readiness.md`; maintainer release judgment remains the final HITL requirement before v0.4 is considered ready to cut.
