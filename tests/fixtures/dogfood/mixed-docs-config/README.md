# Mixed Docs Config Fixture

This fixture represents a repository with release documentation, assistant guidance,
and configuration files but little source code.

Run local checks with:

```bash
make verify
```
