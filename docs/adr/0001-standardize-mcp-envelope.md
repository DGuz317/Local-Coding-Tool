# Standardize the v0.2 MCP response envelope

RepoLens v0.2 standardizes the existing v0.1 MCP response envelope instead of replacing it with the draft shape from the v0.2 plan. The envelope is an assistant-facing contract, so v0.2 should tighten consistency, freshness, truncation, limits, confidence, evidence, and structured errors without introducing unnecessary compatibility churn.
